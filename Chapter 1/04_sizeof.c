#include<stdio.h>

int main() {
    printf("1. Size of int: %zu bytes\n", sizeof(int));
    printf("2. Size of char: %zu bytes\n", sizeof(char));
    printf("3. Size of float: %zu bytes\n", sizeof(float));
    return 0;
}
